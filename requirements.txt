# Core toolkit runs on the Python standard library for most parsers.
# Python 3.8+.
#
# Positioning: No Apify. No captcha cracking. Local direct HTTP.
# (Optional browser only where a site requires reCAPTCHA for phones.)

# Optional — required for Kolesa.kz full phone collection:
playwright>=1.40.0
