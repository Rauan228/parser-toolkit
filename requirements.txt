# Currently the toolkit runs on the Python standard library only
# (urllib, json, csv, ssl, argparse, unittest, …).
# Python 3.8+ is enough — no third-party packages required yet.
#
# Positioning: No Apify. No browser automation. Local direct HTTP.
# (If a future parser needs a dependency, add it here without changing that.)
