# Install the package (recommended):
#   pip install -e .
#   pip install -e ".[kolesa]"   # + Playwright for Kolesa phones
#   playwright install chromium
#
# Or bare deps:
#   pip install -r requirements.txt
#
# Most parsers need only the stdlib. Playwright is optional.

playwright>=1.40.0
