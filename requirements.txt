# Most parsers: Python standard library only (Python 3.8+).
#
# Positioning:
#   No Apify. Parsers run locally, primarily over direct HTTP.
#   Browser automation only where the source requires it.
#
# Optional — required for Kolesa.kz full phone collection:
playwright>=1.40.0
