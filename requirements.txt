# All parsers use only the Python standard library
# (urllib, json, csv, ssl, argparse, unittest, …).
# Python 3.8+ is enough — no third-party packages required.
#
# No Apify SDK. No Playwright/Selenium. No paid API clients.
